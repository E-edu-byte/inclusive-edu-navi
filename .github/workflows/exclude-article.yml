# 記事除外（ブラックリスト）ワークフロー
# GitHubのActions画面から手動実行できます
# 複数URLを改行区切りで入力可能
name: Exclude Article

on:
  workflow_dispatch:
    inputs:
      urls:
        description: '除外する記事のURL（複数の場合は改行区切り）'
        required: true
        type: string

permissions:
  contents: write

env:
  TZ: Asia/Tokyo

jobs:
  exclude-article:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

      - name: Exclude articles
        run: |
          echo "${{ inputs.urls }}" | python scripts/exclude-article.py --stdin

      - name: Check for changes
        id: git-check
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -m "chore: 記事を永久除外 - $(date +'%Y-%m-%d %H:%M' -d '+9 hours')"
          git push origin main

      - name: Trigger deploy
        if: steps.git-check.outputs.changed == 'true'
        uses: peter-evans/repository-dispatch@v3
        with:
          event-type: article-excluded
